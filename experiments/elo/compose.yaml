services:
  bga-spider:
    image: registry.gitlab.com/recommend.games/blog
    build:
      context: .
      dockerfile: Dockerfile
    container_name: elo-bga-spider
    # Mount local results so spider output persists on the host.
    volumes:
      - ./results:/app/results
