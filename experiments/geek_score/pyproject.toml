[tool.poetry]
name = "geek-score"
version = "0.1.0"
description = "Predict BGG geek scores"
authors = ["Markus Shepherd <markus@recommend.games>"]
readme = "README.md"
packages = [{include = "geek_score"}]

[tool.poetry.dependencies]
python = "~3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
